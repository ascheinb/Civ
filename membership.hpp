#ifndef MEMBERSHIP_HPP
#define MEMBERSHIP_HPP
#include <vector>
#include <string.h>
#include "random.hpp"
#include "names.hpp"

class Membership{
    public:
    int id;

    Membership(int id) : id(id) {}
};

#endif
